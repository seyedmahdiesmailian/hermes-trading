{
 "last_run_utc": "2026-08-31T19:20:02.262939+00:00",
 "recent_commits": [
  "3953580 autopilot: b49 \u2014 fail-safe scripts must not be indistinguishable from no-op scripts: engines/selfcheck.py seam (fail(section,exc) = no-op normally, RuntimeError naming the section under --self-check/HERMES_SELFCHECK=1; self-check also suppresses Telegram send + report STATE write so a probe can't spam ops or consume a pending report); wired into autopilot_harvest, autopilot_digest (6 guard blocks), autopilot_report, dirty_work git call; autopilot.sh runs the harvester WITH --self-check, captures HARVEST_RC immediately (b44), logs BROKEN loudly, never blocks \u2014 blind || true gone; 17 tests in test_b49_selfcheck.py demonstrate the ambiguity (PATH-stripped harvester: normal rc0+empty == clean, selfcheck rc!=0) + b36-shape firing + AST tripwire proven to bite; lesson: ast.walk is a generator, iterating twice makes the 2nd check vacuous; new todos b50 (verify_head must run FULL suite on clean checkout \u2014 920ed0d shipped b45 tests without source fixes, HEAD was 3-RED until this run's harvest) and b45 note. 357 green, live cycle rc=0",
  "3f95cf8 harvest: b45 source fixes were left uncommitted while its tests shipped in 920ed0d \u2014 HEAD was BROKEN on clean checkout (3 tests RED in git-archive tree: mixed-grade and position-count fixes existed only in the dirty working tree, b42 bug class 4th occurrence); suite green 340",
  "920ed0d b45: double-sell incident fixes \u2014 mixed alignment is grade C (blocked), position count from positions endpoint (MAX_OPEN_POSITIONS gate was dead); regression tests + probes",
  "30ee2be autopilot: b48 \u2014 test-seam env vars must redirect STATE, never CODE: AST tripwire (tests/test_b48_env_seam_code_vs_state.py) forbids any HERMES_*_ROOT env value (direct, alias chain, Path/f-string wrappers, 'from sys import path' shape) from reaching a sys.path.insert/append in any production script; audit result CLEAN (digest inserts file-derived ROOT, harvest has fixed CODE_ROOT / env SCAN_ROOT split); pinned with minimum-findings count, CODE_ROOT-hardcoded assertion, and a behavioural replay of b47's silent-death mode (harvester vs a shadowing engines/ that raises on import -> STEP 0 still prints; injected first-draft bug -> 3 RED incl. empty-string signature). new todo b49: fail-safe scripts must not be indistinguishable from no-op scripts (--self-check seam). 335 green, live cycle rc=0",
  "0a02223 autopilot: b47 \u2014 harvest abandoned work before picking an item: scripts/autopilot_harvest.py prints a STEP 0 block from engines/dirty_work.scan (files+status+age, verify-suite-green -> own harvest: commit -> verify_head.sh -> only then top todo); autopilot.sh prepends it to the agent PROMPT, fail-safe || true; 9 tests in test_b47_harvest.py replay the exact b36 shape end-to-end (staged AM 3h -> STEP 0; clean/fresh/non-repo -> silent) + bash prepend semantics + ordering tripwire; lesson: HERMES_REPO_ROOT seam must redirect the SCAN root only, never sys.path (first draft's import died inside the fail-safe = silent empty output); new todo b48 (AST tripwire for that class). 325 green, live cycle rc=0"
 ]
}