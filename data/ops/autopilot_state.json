{
 "last_run_utc": "2026-08-31T20:04:50.072930+00:00",
 "recent_commits": [
  "e70df35 autopilot: b45 \u2014 git_sync must never push an UNVERIFIED HEAD: engines/head_verify.py gains decide_push (pure: fresh OK-for-this-sha pushes; BROKEN/TIMEOUT blocks; sha-mismatch blocks 'not yet verified'; missing/stale/unparseable FAILS OPEN \u2014 a dead verifier must not freeze the only backup \u2014 reason always logged, three outcomes three signatures) + push-gate CLI (stdout contract JSON: paths WARN routed to stderr; exit 0=push/1=block) + git_sync.sh wired (idle ahead=0 exits before the gate; blocked = log PUSH BLOCKED + no push, no Telegram re-page \u2014 verify_head.sh is the one voice; gate crash = fail-open logged). LESSON pinned by the throwaway-repo end-to-end replay: the gate must resolve HEAD from the CALLER's cwd, not the code's repo \u2014 first draft certified the wrong tree (b46/b48 disease from the git side). 23 tests in tests/test_b45_push_gate.py incl. real local bare origin: BROKEN stays unpushed, OK pushes, stale BROKEN fail-opens. b50 marked done (shipped via harvest c607fec, VERDICT=OK tests=369 stamped); backlog header + autopilot.sh step 4b text updated to the b50 reality; new todo b51 (crashed run leaves HEAD unverified -> verify at run start). 392 green, live cycle rc=0",
  "c607fec harvest: b50 \u2014 verify_head must run the FULL suite inside HEAD, not just import checks (found while harvesting b45: 920ed0d shipped b45 tests WITHOUT its source fixes, everything imported, 3 tests RED, old verdict said OK). engines/head_verify.py runs the whole suite in a clean DETACHED WORKTREE of the ref (git archive cannot: no .git dir, so the b42/b44/b46 git checks certify the wrong tree) with a minimal env + HERMES_DATA_ROOT inside the throwaway tree, HERMES_B50_NESTED recursion guard, and stamps data/ops/head_verified.json (the seam b45's push gate will read). Prerequisite shipped too: tests/ is now LOCATION-INDEPENDENT \u2014 every module derived its root from __file__ instead of sys.path.insert(0,'/home/ai/hermes-trading'), which made discovery die inside a checkout with 'module incorrectly imported'; pinned by tests/test_b50_suite_is_location_independent.py (AST tripwire distinguishing code from a string of code, dead-exemption check, real-history BROKEN replay of 920ed0d, worktree-leak check, script-wiring check). verify_head.sh: import checks first (specific ModuleNotFoundError shape), then full suite; alert carries the verifier note.",
  "3953580 autopilot: b49 \u2014 fail-safe scripts must not be indistinguishable from no-op scripts: engines/selfcheck.py seam (fail(section,exc) = no-op normally, RuntimeError naming the section under --self-check/HERMES_SELFCHECK=1; self-check also suppresses Telegram send + report STATE write so a probe can't spam ops or consume a pending report); wired into autopilot_harvest, autopilot_digest (6 guard blocks), autopilot_report, dirty_work git call; autopilot.sh runs the harvester WITH --self-check, captures HARVEST_RC immediately (b44), logs BROKEN loudly, never blocks \u2014 blind || true gone; 17 tests in test_b49_selfcheck.py demonstrate the ambiguity (PATH-stripped harvester: normal rc0+empty == clean, selfcheck rc!=0) + b36-shape firing + AST tripwire proven to bite; lesson: ast.walk is a generator, iterating twice makes the 2nd check vacuous; new todos b50 (verify_head must run FULL suite on clean checkout \u2014 920ed0d shipped b45 tests without source fixes, HEAD was 3-RED until this run's harvest) and b45 note. 357 green, live cycle rc=0",
  "3f95cf8 harvest: b45 source fixes were left uncommitted while its tests shipped in 920ed0d \u2014 HEAD was BROKEN on clean checkout (3 tests RED in git-archive tree: mixed-grade and position-count fixes existed only in the dirty working tree, b42 bug class 4th occurrence); suite green 340",
  "920ed0d b45: double-sell incident fixes \u2014 mixed alignment is grade C (blocked), position count from positions endpoint (MAX_OPEN_POSITIONS gate was dead); regression tests + probes"
 ]
}