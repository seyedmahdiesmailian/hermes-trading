{
 "last_run_utc": "2026-09-03T06:12:36.661724+00:00",
 "recent_commits": [
  "cd38a62 b61: exit-geometry expectancy audit (6 arms x 6500 M5 bars, R-multiple stats) + trade-vs-plan root-cause scripts. Verdict: current b60 ladder (TP1=midpoint, full close) is the BEST arm - net 1670$, exp 0.854R, maxDD 3.1R; hold-to-final is 2.5x worse (659$). Backtest trade_log now carries orig_sl for R math.",
  "7aaac4d autopilot: b66-follow-up \u2014 ONE shape-safe /api/positions reader (engines/bridge_payload) \u2014 found by the b66 clean-worktree verify: with no .env there is no bridge token, so _get() returns {ok:false, error:HTTP_401, data:{raw:...}} and dashboards.trade_home() iterated the DICT, hit string keys and died (AttributeError 'str' has no 'get') -> panel fell back to an error card, b38 fallback test RED. b66 did not break it, it REMOVED the accident that hid it: the old ROOT literal made dashboards load the PRODUCTION .env even inside an isolated worktree, so verification always had a token and always saw a list. Real production exposure: any auth blip/MT5 error page renders an error card on the operator's home panel, and on the SIGNAL path the same shape degraded open_positions to 0 (already_in_position gate blind \u2014 the b65 auth-as-empty-account class). Healed 4 consumers (dashboards x3, signal_listener, position_daemon live-map, hermes_runtime delegates) onto positions_list/position_count with the daemon's no-ok leniency preserved (reading it empty would falsely report tracked tickets CLOSED). 15 tests incl. the exact 401 payload replayed through the real panels + an AST scan banning hand-rolled .get('data') iteration in the four consumers; anti-vacuity proves the OLD comprehension raises on that payload. 501 green, live cycle rc=0.",
  "a1347bd harvest: b66 absolute repo-path literal heal (20 scripts + notifier + engines/paths repo_root()/repo_file() + 4 cron .sh BASH_SOURCE derivation + env seams) \u2014 previous run died before committing; STEP-0 harvest verified: 15 b66 tests green (fixed BFS-order flake in repo_path_hits_py: ast.walk yields f-string constants after later top-level statements, replay assertion now sorts by line), full suite green except the b42 untracked-file check that this commit itself resolves",
  "d070019 autopilot: b64 literal-host tripwire (harvested from interrupted run) + b65 env-bootstrap heal \u2014 b64: sensitive-host set DERIVED from .env.example (RFC1918 by rule + documented hosts by contract), both languages, per-file allowlist with written reasons + dead-exemption liveness; _check_bridge healed onto bridge_client (DEPLOY.md smoke test must follow the documented host), _deploy_bridge onto the b62 chain + WIN_USER, three dead surgery probes retired to legacy_removed/ (two mutate the Windows box). PROVEN to bite: HEAD _check_bridge restored -> 2 RED; fresh synthetic probe -> instant violation. b65 (found by the b64 audit, the class every env-READ scan is blind to): cli.py constructed BridgeClient with NO load_dotenv -> tokenless 401s rendered as 'Open Positions: 0' (auth failure as empty account, verified live), cli.py run's DRY_RUN flag was INVERTED (bare run = LIVE, --live = dry-run), hermes_runtime.main() had no bootstrap + cycle() default dry_run=False = live-order-by-accident path. All healed to dry-run-safe defaults (hermes_master untouched); 8 behavioural tests (subprocess replays through real argparse/cmd_status/main, stubbed bridge). 471 green, live cycle rc=0 (monitor, no_trade). New todo b66: absolute repo-path literals, same class one level down.",
  "d73ce55 autopilot: b63 documented-host drift check \u2014 parse_host_drift() pins the host embedded in HERMES_BRIDGE_URL to HERMES_WIN_IP in .env.example + real .env (bridge_client lets the URL win when set, so a half-done host migration silently keeps trading on the OLD host \u2014 the b62 shape one level up, invisible to the knob-scope scan by design); consumer tripwire scan_literal_bridge_url_defaults(): a HERMES_BRIDGE_URL read may not carry a literal default, the fallback must DERIVE from HERMES_WIN_IP. AUDIT FOUND A LIVE WORSE CASE: bridge_health_monitor (cron */5, pages ops on bridge death) hardcoded the URL in two module-level literals and read NO env at all \u2014 now bridge_urls() resolves at call time with bridge_client precedence; weekly_report literal default healed the same way. 7 new tests incl. behavioural precedence replays in fresh subprocesses; PROVEN to bite (literal restored + half-migrated example -> 6 RED). SIDE FINDING -> todo b64 (hardcoded IPs in probe scripts, invisible to env-read scans). 451 green, live cycle rc=0 (monitor, no_trade)."
 ]
}